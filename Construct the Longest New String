class Solution {
    public int longestString(int x, int y, int z) {
        
        if(x==y){
            return (x*2)+(y*2)+(z*2);
        }else if(x<y){
            return (x*2)+((x+1)*2)+(z*2);
        }else{
            return (y*2)+((y+1)*2)+(z*2);
        }

       
    }
}
